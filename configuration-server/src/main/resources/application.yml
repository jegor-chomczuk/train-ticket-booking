server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jegor-chomczuk/train-ticket-booking
          search-paths: configuration-files
          try-master-branch: false
          skip-ssl-validation: true
